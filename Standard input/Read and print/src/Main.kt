import java.util.Scanner

fun main() {
    val scanner = Scanner(System.`in`)
    var cin = scanner.nextLine()
    println(cin)
    cin = scanner.nextLine()
    println(cin)
    cin = scanner.next()
    println(cin)
    cin = scanner.next()
    print(cin)
}